# Redrob Candidate Ranker — Dependencies

# Core ranking (Stage 1): Python stdlib only
# No install needed for rule-based stage.

# Semantic reranking (Stage 2):
sentence-transformers>=2.2.0   # all-MiniLM-L6-v2 bi-encoder
numpy>=1.24                    # cosine similarity computation

# Install with:
#   pip install sentence-transformers numpy
#
# Then download model ONCE (works offline after):
#   python download_model.py
#
# Python 3.9+ required